import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Sync extends Component {
  render() {
    return <h1>Sync</h1>;
  }
}

Sync.propTypes = {};

Sync.defaultProps = {};

export default Sync;
